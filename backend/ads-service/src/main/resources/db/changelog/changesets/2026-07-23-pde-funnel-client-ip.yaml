databaseChangeLog:
  - changeSet:
      id: 2026-07-23-pde-funnel-client-ip-001
      author: codex
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - dbms:
            type: mysql
        - tableExists:
            tableName: pde_funnel_event
        - not:
            columnExists:
              tableName: pde_funnel_event
              columnName: client_ip
      changes:
        - sql:
            splitStatements: true
            stripComments: true
            sql: |
              ALTER TABLE pde_funnel_event
                ADD COLUMN client_ip VARCHAR(45) NULL AFTER page_url;
