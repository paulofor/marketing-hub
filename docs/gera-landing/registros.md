# Registros — Gera Landing

> Orientação: todos os registros deste documento devem sempre incluir **data e hora no fuso UTC-3**.
> Neste documento que o arquivo `docs/gera-landing/registros.md` segue política de **append-only** (não pode ter nenhuma linha apagada; apenas inserções).

- 2026-05-01 23:33:32 (UTC-3): criado o pacote `com.marketinghub.geralanding` no backend (`ads-service`) para centralizar os componentes do módulo Gera Landing.
- 2026-05-02 00:00:00 (UTC-3): adicionados o card **Gera WireFrame** e o botão **Iniciar** na aba "Gera landing" da tela de experimento no frontend; o botão agora envia POST para `/api/experiments/{experimentId}/geralanding/wireframe/start` no backend (package `com.marketinghub.geralanding`) com retorno `202 Accepted` sem processamento adicional neste momento.

- 2026-05-03 09:00:00 (UTC-3): no `ai-worker`, refatorado `GeraLandingService` para expor métodos tipados de leitura de `campaignAngle`, `adCopy`, `adImageBriefing` e `experimentMetadata` com DTOs existentes; criado `LandingPageWireframeDto` no pacote `geralanding` para encapsular `landingPageWireframe`.
- 2026-05-03 10:45:00 (UTC-3): integração do disparo do botão **Iniciar** com geração real de wireframe no backend (`GeraLandingWireframeController`), delegando para `ExperimentPipelineGenerationService.generate(..., LANDING_PAGE_WIREFRAME, ...)`; no `ai-worker`, adicionada montagem de prompt por etapa em `prompts/geralanding/{etapa}.md` com resolução de placeholders `{prompt-*}` e `{dados-*}` e registro do prompt final no banco via endpoint `/api/ai/generations/internal` (tabela `ai_worker_generation`) com chave de rastreio no formato `exp:{experimentId}|etapa:{etapa}|exec:{execucaoId}|job:{jobId}`.
- 2026-05-03 11:05:00 (UTC-3): ajuste de aderência de teste unitário no `ai-worker`: o cabeçalho do prompt base `prompts/geralanding/regras-globais.md` foi alterado de `Regras globais:` para `REGRAS GLOBAIS:` para compatibilizar com a asserção de `GeraLandingServiceTest.deveMontarPromptEtapaComPromptEDados`.
- 2026-05-03 14:20:00 (UTC-3): adicionada documentação Swagger em `docs/gera-landing/swagger-gera-landing-wireframe.yaml` para a etapa de wireframe com envio explícito de `prompt` (prompt montado), `stageCode` (código da etapa) e `experimentCode` (código do experimento).

- 2026-05-03 14:55:00 (UTC-3): no backend (`ads-service`), endpoint `POST /api/experiments/{experimentId}/geralanding/wireframe/start` passou a receber payload validado (`stageCode`, `prompt.templateId`, `prompt.content`) e registrar execução em banco na tabela `gera_landing_stage_execution` (chave composta: `experiment_id`, `stage_code`, `execution_requested_at`), permitindo reaproveitamento para outras etapas além de wireframe e mantendo rastreabilidade por data-hora de execução.
- 2026-05-03 15:05:00 (UTC-3): consolidação final do PR: persistência de execuções do Gera Landing estabilizada em estrutura genérica por etapa (`gera_landing_stage_execution`), com validação de payload no endpoint de início de wireframe e rastreabilidade por chave composta (`experiment_id`, `stage_code`, `execution_requested_at`), deixando a base pronta para inclusão das próximas etapas do fluxo Gera Landing.
- 2026-05-03 15:25:00 (UTC-3): corrigida a sintaxe YAML do changelog `2026-05-03-gera-landing-wireframe-execution.yaml` no backend (`ads-service`), ajustando `preConditions` para sequência válida (`onFail`, `onError`, `dbms:mysql` e `not tableExists`) e eliminando a falha de parse do Liquibase no GitHub Actions.
- 2026-05-03 16:40:00 (UTC-3): implementação do handoff backend↔worker para auditoria de prompt montado no Gera Landing: no backend (`ads-service`) foi criado o endpoint interno `POST /api/internal/geralanding/stage-executions` (`GeraLandingInternalController`) com DTO validado (`GeraLandingWorkerPromptRequest`) e serviço `GeraLandingStageExecutionService.registerWorkerPromptExecution(...)`; no `ai-worker`, `ExperimentPipelineBackendClient` ganhou `registerGeraLandingPrompt(...)` e `GeraLandingService` passou a enviar `experimentId`, `stageCode`, `executionId` e `promptContent` para esse endpoint após montar o prompt.
- 2026-05-03 18:35:00 (UTC-3): início do wireframe ajustado para registrar execução inicial em `gera_landing_stage_execution` com os novos campos `status` e `id_job` (status inicial `INICIADO` e `idJob` UUID), retornando `202 Accepted` com `{ idJob, status }` para o frontend exibir código e status ao usuário no feedback de sucesso.
- 2026-05-03 18:55:00 (UTC-3): adicionadas colunas dedicadas de ciclo de vida em `gera_landing_stage_execution` para separar os momentos de `created_at` (criação), `processing_started_at` (início de processamento) e `completed_at` (conclusão); nesta etapa o backend passou a persistir explicitamente `created_at` na criação do registro, mantendo os demais marcos preparados para evolução posterior.
- 2026-05-04 09:30:00 (UTC-3): no `ai-worker`, criado no pacote `com.marketinghub.worker.geralanding` um novo ciclo de polling para execuções pendentes via `GeraLandingExecutionScheduler` (cron a cada 1 minuto por configuração), `GeraLandingExecutionService` e `GeraLandingBackendClient`, consultando o endpoint interno `GET /api/internal/geralanding/stage-executions/pending?limit=...` sem alterar classes do pacote `experimentpipeline`.
- 2026-05-04 09:45:00 (UTC-3): no processamento das pendências retornadas pelo backend, adicionada regra explícita no `ai-worker` para a etapa `landing-page-wireframe`: ao identificar `stageCode` de wireframe, o worker chama o método já existente de montagem de prompt (`montarERegistrarPromptEtapa(...)`), com logs operacionais para itens ignorados (etapa diferente/registro inválido) e para falhas por execução.
- 2026-05-04 10:10:00 (UTC-3): implementado handoff de prompt montado do `ai-worker` para o backend no domínio `geralanding` através de endpoint interno dedicado `POST /api/internal/geralanding/stage-executions/{idJob}/receive-prompt`; no backend (`ads-service`) o recebimento grava o conteúdo em `gera_landing_stage_execution.prompt` e atualiza o `status` do registro para `RECEBIDO`.
- 2026-05-04 10:20:00 (UTC-3): no backend (`ads-service`), criada migração Liquibase incremental `2026-05-04-gera-landing-add-prompt-column.yaml` (MySQL 5.7) e inclusão no `db.changelog-master.yaml` para adicionar a coluna `prompt` (`LONGTEXT`) na tabela `gera_landing_stage_execution`, mantendo compatibilidade com evolução do fluxo de geração por etapas.
- 2026-05-04 22:45:00 (UTC-3): corrigida a resolução de changelog do Liquibase no backend (`ads-service`) após `ChangeLogParseException` informando arquivo não encontrado; no `db.changelog-master.yaml` foi adicionado `relativeToChangelogFile: true` no include de `changesets/2026-05-04-gera-landing-add-prompt-column.yaml`, alinhando o primeiro include ao padrão já usado nos demais changesets.
- 2026-05-04 22:58:00 (UTC-3): corrigida a sintaxe de `preConditions` no changelog `2026-05-04-gera-landing-add-prompt-column.yaml` (backend/ads-service), convertendo os campos `onFail` e `onError` para itens de sequência YAML no mesmo nível das demais precondições (`dbms:mysql` e `not columnExists`), eliminando o `ParserException` do Liquibase durante o `validate`.
- 2026-05-03 23:40:00 (UTC-3): no backend (`ads-service`), criado o endpoint interno `GET /api/internal/geralanding/stage-executions/pending` para listar execuções com status `INICIADO`, retornando payload mínimo para o worker com `idJob` e `stageCode`; adicionados ajuste no repositório/serviço para buscar até 20 pendências ordenadas por `execution_requested_at` e teste de controller cobrindo o novo contrato.
- 2026-05-04 23:20:00 (UTC-3): ajuste corretivo no fluxo de pendências do Gera Landing para garantir `experimentId` em todos os itens retornados por `GET /api/internal/geralanding/stage-executions/pending`; no backend (`ads-service`), `GeraLandingPendingExecutionResponse` passou a incluir `experimentId` e o `GeraLandingStageExecutionService.listPendingExecutions()` agora mapeia explicitamente `execution.getExperimentId()`, evitando `experimentId=null` no `ai-worker` e preservando a rastreabilidade obrigatória do experimento em todo o pipeline.
- 2026-05-04 23:35:00 (UTC-3): corrigido erro de compilação em teste do backend (`ads-service`) após evolução do contrato de `GeraLandingPendingExecutionResponse` para incluir `experimentId`; em `GeraLandingInternalControllerTest.shouldListPendingExecutions` o construtor passou a receber `experimentId` (`77L`) e a asserção do JSON passou a validar `experimentId`, `idJob` e `stageCode` no endpoint de pendências.
- 2026-05-04 11:05:00 (UTC-3): identificado em produção, via MCP (`information_schema.columns`), que `gera_landing_stage_execution.prompt` estava com tipo físico `tinytext` em vez de `LONGTEXT`, causando falha `500` no `POST /api/internal/geralanding/stage-executions/{idJob}/receive-prompt` ao persistir prompts longos do `ai-worker`; no backend (`ads-service`) foi criado o changeset incremental `2026-05-04-gera-landing-fix-prompt-column-type.yaml` com `modifyDataType` para `LONGTEXT` e inclusão no `db.changelog-master.yaml` para correção retroativa de ambiente.
- 2026-05-04 13:40:00 (UTC-3): no backend (`ads-service`), registrado ajuste operacional de diagnóstico no fluxo `POST /api/internal/geralanding/stage-executions/{idJob}/receive-prompt`: foram adicionados logs estruturados em `GeraLandingStageExecutionService.receivePrompt(...)` antes do ponto de falha para capturar `idJob`, `experimentId`, `stageCode`, tamanho do prompt, resultado do lookup da execução e confirmação de persistência/atualização de status; em conjunto, o contrato interno foi mantido com `idJob` textual (`String`) para alinhamento com armazenamento em `CHAR(36)` na tabela `gera_landing_stage_execution`.
- 2026-05-04 14:06:28 (UTC-3): no `ai-worker`, corrigido o contrato de leitura de pendências do Gera Landing para tratar `idJob` como texto no endpoint interno `GET /api/internal/geralanding/stage-executions/pending`, removendo desserialização como `UUID` que gerava `DecodingException`; adicionalmente, adicionado guard clause no `GeraLandingExecutionService` para ignorar e logar pendências inválidas sem `idJob`/`stageCode`, evitando chamadas de retorno com identificador ausente para `POST /api/internal/geralanding/stage-executions/{idJob}/receive-prompt`.
- 2026-05-04 14:59:57 (UTC-3): ajuste corretivo no handoff de prompt do Gera Landing para evitar falha de roteamento com `idJob` no path: no `ai-worker`, `GeraLandingBackendClient.receivePrompt(...)` passou a chamar `POST /api/internal/geralanding/stage-executions/receive-prompt` enviando `idJob` no body JSON; no backend (`ads-service`), `GeraLandingInternalController` ganhou endpoint body-based compatível (`GeraLandingPromptReceiveDirectRequest`) mantendo o endpoint legado `/{idJob}/receive-prompt` para retrocompatibilidade, com teste de controller cobrindo o novo contrato.
- 2026-05-04 15:40:00 (UTC-3): registro de consolidação do ajuste de robustez no recebimento de prompt da etapa wireframe: no backend (`ads-service`) o serviço `GeraLandingStageExecutionService.receivePrompt(...)` passou a incluir fallback de busca por `(experimentId, stageCode)` quando o lookup por `idJob` não encontra execução, e no `ai-worker` o cliente `GeraLandingBackendClient.receivePrompt(...)` foi alinhado para envio do `idJob` no path do endpoint `POST /api/internal/geralanding/stage-executions/{idJob}/receive-prompt` (mantendo `experimentId`, `stageCode` e `prompt` no body), garantindo aderência de contrato entre caller e endpoint.
- 2026-05-04 23:55:00 (UTC-3): correção de documentação solicitada: o registro do ajuste no `ai-worker` sobre remoção de `execucaoId` em `GeraLandingService` foi consolidado no arquivo canônico `docs/gera-landing/registros.md` (caminho correto), mantendo rastreabilidade do envio de prompt com `job.id().toString()` no contexto do serviço.
- 2026-05-04 16:05:00 (UTC-3): correção de horário do registro anterior sobre consolidação do caminho de documentação; considerar este como o horário válido do ajuste solicitado para `docs/gera-landing/registros.md`.
- 2026-05-04 19:20:00 (UTC-3): no backend (`ads-service`), alterada a estratégia de chave primária da tabela `gera_landing_stage_execution` para usar `id_job` como PK única via changeset incremental Liquibase (`2026-05-04-gera-landing-id-job-primary-key.yaml`), com alinhamento do mapeamento JPA (`GeraLandingStageExecution`) e do repositório para identificador textual (`String`), removendo a classe de chave composta legada.
- 2026-05-04 22:10:00 (UTC-3): registro de governança solicitado: documentado neste arquivo o trabalho do PR `chore(db): garantir id_job como chave primária em gera_landing_stage_execution`, incluindo criação do changeset incremental `2026-05-05-gera-landing-ensure-id-job-primary-key.yaml` e inclusão no `db.changelog-master.yaml`, mantendo o tipo de `id_job` inalterado.
- 2026-05-04 22:50:00 (UTC-3): solicitado ajuste pós-review para manter aderência total ao banco como fonte da verdade: consolidado no backend (`ads-service`) o mapeamento de `id_job` como `BINARY(36)` (entidade/repositório/serviço com conversão String↔byte[] para contratos de API), atualização dos testes unitários relacionados e alinhamento do documento canônico `docs/gera-landing/modelo-canonico-gera-landing.md` ao schema operacional atual; este registro documenta explicitamente o trabalho do PR "Persist id_job as binary and adapt repository, service, tests and docs".
- 2026-05-04 22:55:00 (UTC-3): registro pós-review do PR "fix: expand gera landing prompt storage to LONGTEXT": no backend (`ads-service`) foi reforçado o armazenamento de prompts longos na entidade `GeraLandingStageExecution` com `@Column(columnDefinition = "LONGTEXT")` para o campo `prompt`, criado o changeset incremental `2026-05-05-gera-landing-prompt-longtext-hardening.yaml` com `modifyDataType` para `LONGTEXT` em `gera_landing_stage_execution.prompt` e incluído no `db.changelog-master.yaml`, mitigando recorrência do erro MySQL 1406 (Data too long for column prompt) em prompts extensos do fluxo Gera Landing.
- 2026-05-05 00:15:00 (UTC-3): correção de aderência do fluxo Worker→Backend no `ai-worker` para preservar rastreabilidade de execução: `GeraLandingExecutionService` passou a encaminhar o `idJob` original recebido do endpoint de pendências para `GeraLandingService.montarERegistrarPromptEtapa(...)`, e o callback `POST /api/internal/geralanding/stage-executions/{idJob}/receive-prompt` agora reutiliza esse mesmo `idJob` (sem gerar novo UUID local para o retorno); também foi adicionada cobertura de teste unitário para garantir o comportamento.

##Finalização do Fluxo de Registro de Prompt Etapa Wireframe

- 2026-05-05 00:57:09 (UTC-3): registro pós-review do PR "Adicionar card de execuções da etapa Gera Landing": no backend (`ads-service`) foi disponibilizado o endpoint `GET /api/experiments/{experimentId}/geralanding/stage-executions` com filtro por `stageCode` (default `landing-page-wireframe`) para listar as últimas execuções da tabela `gera_landing_stage_execution` ordenadas da mais recente para a mais antiga, retornando `idJob`, `status` e `executionRequestedAt`; no frontend (`ExperimentDetailPage`, aba **Gera landing**) foi adicionado o card "Execuções da etapa" com tabela contendo **Job ID**, **Status** e **Data-hora**, incluindo recarregamento da lista após acionar o botão **Iniciar**.

- 2026-05-05 09:30:00 (UTC-3): registro pós-review do PR "Track GeraLanding OpenAI lifecycle data across worker and backend": no `ai-worker`, o fluxo do pipeline passou a registrar o prompt completo antes da geração e a enviar o resultado final ao backend com resposta do modelo, tokens e custo; no backend (`ads-service`), foram adicionados contrato/endpoints internos para receber `openAiJobId` no recebimento de prompt e para receber resultado final (`/api/internal/geralanding/stage-executions/{idJob}/receive-result`), além da evolução de `GeraLandingStageExecution` e do serviço para persistir `openai_job_id`, `model_response`, `input_tokens`, `output_tokens`, `cost_usd`, atualizar status para `EM_PROCESSAMENTO`/`CONCLUIDO` e fechar com `completed_at`.

- 2026-05-05 10:25:11 (UTC-3): ajuste pós-review no backend (`ads-service`) para restaurar compilação dos testes do módulo Gera Landing após evolução do contrato `GeraLandingPromptReceiveRequest` (inclusão de `openAiJobId`); em `GeraLandingStageExecutionServiceTest`, as duas instanciações do record foram atualizadas para a assinatura de 4 parâmetros com `openAiJobId=null` nos cenários em que o campo não é relevante.

- 2026-05-05 11:05:00 (UTC-3): registro pós-review solicitado para garantir a persistência do fluxo Gera Landing concentrada em `gera_landing_stage_execution`: no backend (`ads-service`) foi adicionado o changeset incremental `2026-05-06-gera-landing-result-columns.yaml` (com preconditions idempotentes) para criar, quando ausentes, as colunas `openai_job_id`, `model_response`, `input_tokens`, `output_tokens` e `cost_usd`, e o arquivo foi incluído no `db.changelog-master.yaml`; no `ai-worker`, `ExperimentPipelineGenerationWorkerService` deixou de registrar `recordGenerationLog(...)` nesse fluxo, mantendo o armazenamento operacional (prompt enviado, resposta recebida, tokens, custo e conclusão) no ciclo de endpoints internos de Gera Landing.

- 2026-05-05 11:40:00 (UTC-3): registro pós-review do PR "Adicionar link por Job ID e página de detalhe para execuções do Gera Landing": no frontend, a lista de execuções da aba **Gera landing** passou a exibir `Job ID` como link para a nova rota `/experiments/:id/geralanding/stage-executions/:jobId`, com tela de detalhe contendo migalha de pão, botão de voltar e exibição dos campos da execução; no backend (`ads-service`), foi criado o endpoint `GET /api/experiments/{experimentId}/geralanding/stage-executions/{idJob}` com serviço/repositório dedicados para consultar e retornar os dados completos de `gera_landing_stage_execution` por `experimentId + idJob`.
- 2026-05-05 14:10:00 (UTC-3): registro solicitado em pós-review do PR "Send GeraLanding prompts to ExperimentPipeline OpenAI and post results to backend": no `ai-worker`, o fluxo de `GeraLandingExecutionService` foi ajustado para executar sequência completa após montagem do prompt (validação de client OpenAI habilitado, envio do prompt para geração, recebimento do payload de conclusão e callback para `/api/internal/geralanding/stage-executions/{idJob}/receive-result` via `GeraLandingBackendClient.receiveResult(...)`), com teste unitário dedicado (`GeraLandingExecutionServiceTest`) cobrindo chamada de geração e registro de resultado.
- 2026-05-05 15:05:00 (UTC-3): registro pós-review solicitado para padronização do payload OpenAI no fluxo Gera Landing (`ai-worker`): `GeraLandingExecutionService` passou a montar `requestBodyJson` no formato Responses API com `model: gpt-5.2`, `input` em lista de mensagens (incluindo `system` e `user` com `input_text`) e `text.format` em `json_schema` com `name: experiment_pipeline_landing_page_copy` e `strict: true`; adicionalmente, `GeraLandingService` registrou log explícito do prompt já tratado antes do envio ao backend.

- 2026-05-05 15:35:00 (UTC-3): registro pós-review solicitado para o fluxo Gera Landing no `ai-worker`: `GeraLandingExecutionService` deixou de depender de `ExperimentPipelineOpenAiClient` e passou a usar cliente dedicado `GeraLandingOpenAiBatchClient` (pacote `com.marketinghub.worker.geralanding`) para envios ao endpoint `/responses` em modo batch lógico; o fluxo mantém a montagem/log do `requestBodyJson` antes do envio e a devolução do resultado para o backend via `receiveResult(...)`.
- 2026-05-05 20:59:46 (UTC-3): registro pós-review solicitado para observabilidade do fluxo Gera Landing no `ai-worker`: em `GeraLandingOpenAiBatchClient.callOpenAi(...)` foram adicionados logs explícitos com a assinatura `callOpenAI` para capturar o payload enviado (`requestPayload`) e a resposta recebida (`responseBody`) da OpenAI, reutilizando `safeJson(...)` para serialização segura no log.

- 2026-05-05 21:40:00 (UTC-3): no `ai-worker`, o método `buildOpenAiRequestBody` em `GeraLandingExecutionService` foi parametrizado para receber `model`, `prompt`, `systemName` e `systemMessage`; a chamada da etapa wireframe passou a enviar esses argumentos explicitamente e o comentário de referência da OpenAI Responses API foi mantido junto à montagem do payload para facilitar auditoria do formato esperado.
- 2026-05-05 22:10:00 (UTC-3): registro pós-review solicitado para o PR de desacoplamento dos tipos do Gera Landing no `ai-worker`: `GeraLandingOpenAiBatchClient.generate(...)` passou a registrar log logo no início com o parâmetro de entrada serializado (`job`) para facilitar diagnóstico de payload recebido antes do parse JSON; também foi consolidada a adoção de DTO/payload próprios do pacote `com.marketinghub.worker.geralanding` (`GeraLandingJobDto` e `GeraLandingJobCompletionPayload`) no fluxo de execução e retorno para o backend.

- 2026-05-05 22:20:00 (UTC-3): no `ai-worker`, adicionada governança arquitetural com ArchUnit para proteger o domínio Gera Landing contra acoplamento indevido: inclusão da dependência de teste `com.tngtech.archunit:archunit-junit5` no `pom.xml` e criação do teste `GeraLandingArchitectureTest` com regra explícita proibindo dependências de `com.marketinghub.worker.geralanding` para `com.marketinghub.worker.experimentpipeline`.
- 2026-05-05 22:45:00 (UTC-3): registro pós-review solicitado para o ajuste no `ai-worker` em `GeraLandingOpenAiBatchClient`: removido o parse de `requestBodyJson` para `Map` no envio à OpenAI e padronizado o envio direto do payload pronto (`openAiRequestBody` em `job.prompt()`) para `/responses` com `Content-Type: application/json`; o payload persistido no retorno também passou a refletir exatamente o corpo bruto enviado.
- 2026-05-05 23:16:00 (UTC-3): no `ai-worker`, mantido `strict: true` no payload OpenAI da etapa `landing-page-wireframe` e extraído o schema para `ai-worker/src/main/resources/prompts/geralanding/landing-page-wireframe-schema.json`; `GeraLandingExecutionService` passou a carregar o schema via classpath (`Resource`) ao montar `text.format.schema`, com ajuste correspondente em teste unitário.
- 2026-05-05 23:33:00 (UTC-3): solicitado registrar o trabalho em documentação operacional; adicionada entrada de registro descrevendo contexto, trabalho executado e resultado da mudança de schema strict no Worker AI.
- 2026-05-05 23:41:00 (UTC-3): correção de caminho de documentação solicitada: o registro foi consolidado no caminho canônico `docs/gera-landing/registros.md`; reforço de governança append-only aplicado (sem exclusão de linhas históricas).

- 2026-05-06 00:05:00 (UTC-3): registro pós-review solicitado para correção do schema da etapa `landing-page-wireframe` no `ai-worker`: o arquivo `ai-worker/src/main/resources/prompts/geralanding/landing-page-wireframe-schema.json` foi atualizado para espelhar o esquema operacional usado no pipeline de experimento, removendo permissividade residual e definindo contrato estrito para `variantLayoutId`, `sectionOrder` (estrutura mínima e campos obrigatórios por seção), `surfaceSpec`, `ctaSlot` e `consistencyChecks`, reduzindo divergências de validação no Worker AI.
- 2026-05-06 00:25:00 (UTC-3): registro pós-review solicitado para o ajuste do modo batch real no `ai-worker` (pacote `com.marketinghub.worker.geralanding`): `GeraLandingOpenAiBatchClient` passou a executar o fluxo oficial assíncrono da OpenAI Batch API com criação de JSONL por job, upload em `/files` com `purpose=batch`, criação do lote em `/batches` (`completion_window: 24h`), polling de status configurável e leitura do `output_file_id` para extração da resposta final, mantendo o envio do resultado consolidado ao backend no contrato já existente do Gera Landing.

- 2026-05-06 00:58:00 (UTC-3): registro pós-review solicitado para observabilidade do resultado OpenAI no fluxo Gera Landing (`ai-worker`): em `GeraLandingExecutionService`, após `openAiClient.generate(openAiJob)`, foram adicionados logs com metadados do retorno (`openAiJobId`, `inputTokens`, `outputTokens`, `costUsd`, tamanhos de `responseContent` e `rawResponse`) e log condicional do `responseContent` quando presente, antes do envio do resultado ao backend via `receiveResult(...)`.
- 2026-05-06 08:00:00 (UTC): no fluxo Gera Landing, adicionada auditoria expandida da execução em `gera_landing_stage_execution` com persistência de `schema_json`, `openai_request_body` e `prompt_markdown_content`; o `ai-worker` passou a enviar esses campos no callback de prompt e também a propagar `openAiJobId` no callback de resultado para preencher `openai_job_id` quando não houver id no primeiro callback.

- 2026-05-06 06:20:00 (UTC-3): ajuste pós-review no frontend da aba **Gera landing** (`frontend/src/pages/experiment/ExperimentDetailPage.tsx`): os jobs não concluídos passaram a permanecer no card com o botão **Iniciar**, a tabela inferior passou a listar apenas execuções concluídas como histórico e o botão **Iniciar** agora fica desabilitado enquanto existir job em execução (além do estado de carregamento da própria ação).

- 2026-05-06 06:40:00 (UTC-3): ajuste pós-review para reduzir atualização desnecessária na UI do Gera Landing: criado suporte no backend (`GET /api/experiments/{experimentId}/geralanding/stage-executions?includeCompleted=false`) para listar apenas jobs não concluídos; no frontend, a aba **Gera landing** passou a consultar jobs pendentes em polling e jobs concluídos sem polling contínuo, evitando recarregar histórico que não muda após conclusão.

- 2026-05-06 07:05:00 (UTC-3): ajuste adicional no frontend da aba **Gera landing** para acompanhar execução ativa por endpoint de detalhe (`GET /api/experiments/{experimentId}/geralanding/stage-executions/{idJob}`): quando houver job em execução, a tela passa a fazer polling do job ativo por `idJob` e, ao detectar status `CONCLUIDO`, recarrega listas pendente/histórico; isso reduz atualizações desnecessárias e foca o refresh no job que pode mudar de estado.
- 2026-05-06 08:25:00 (UTC-3): ajuste pós-review no frontend da tela de experimento (`frontend/src/pages/experiment/ExperimentDetailPage.tsx`) para corrigir falha de renderização (React minified error #310): os retornos condicionais de `isLoading`/`!data` foram reposicionados para depois da declaração dos hooks (`useGeraLandingStageExecutionDetail` e `useEffect` subsequente), garantindo ordem estável de hooks entre renders e restabelecendo o funcionamento da aba **Gera landing**.

- 2026-05-06 12:55:00 (UTC-3): registro pós-review solicitado para ajuste de timeout no Worker AI (`ai-worker`) no fluxo Gera Landing: `GeraLandingOpenAiBatchClient` passou a usar `openai.batch-timeout` com padrão `PT30M` para controlar o tempo máximo de espera do batch da OpenAI, substituindo o limite fixo anterior de tentativas de polling; o cálculo de tentativas agora deriva de `batchTimeout/pollInterval`, a mensagem de timeout passou a incluir a duração configurada, e a configuração/documentação foram alinhadas para 30 minutos (`application.properties`, `docs/canonical/ai-workers-canon.v1.md` e `ai-worker/README.md`).

- 2026-05-06 10:20:00 (UTC-3): ajuste pós-review no frontend da aba **Gera landing** (`frontend/src/pages/experiment/ExperimentDetailPage.tsx`) para refinar a exibição de custos conforme solicitação: a coluna **Custo** foi mantida apenas na tabela de **Histórico de execuções concluídas** (por linha) e o total agregado das execuções concluídas foi mantido no destaque superior do card **Gera WireFrame**; também foi atualizado o contrato tipado da listagem (`costUsd` em `GeraLandingStageExecutionItem`) para consumir o valor retornado pela API quando disponível.

- ##Finalização do Fluxo de Gera Wireframe

- 2026-05-06 09:35:00 (UTC-3): registro pós-review no frontend da aba **Gera landing** (`frontend/src/pages/experiment/ExperimentDetailPage.tsx`): corrigida a exibição de custo no histórico e totalização do card **Gera WireFrame** com resolução robusta de campos de custo (`costUsd`, `totalCostUsd`, `cost`, `totalCost`) e parsing seguro de valores string; mantida a atualização automática do histórico ao término do job sem recarregar a página.
