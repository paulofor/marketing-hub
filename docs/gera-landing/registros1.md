# Registros — Gera Landing

> Orientação: todos os registros deste documento devem sempre incluir **data e hora no fuso UTC-3**.
> Neste documento que o arquivo `docs/gera-landing/registros1.md` segue política de **append-only** (não pode ter nenhuma linha apagada; apenas inserções).

- 2026-05-09 14:40:00 (UTC-3): registro pós-review solicitado para melhoria de diagnóstico de falhas OpenAI Batch no fluxo Gera Landing (`ai-worker`). No `GeraLandingOpenAiBatchClient`, o retorno de `/batches/{id}` passou a mapear também `error_file_id`; quando um batch conclui sem `output_file_id`, o worker agora tenta baixar o conteúdo do arquivo de erro (`/files/{error_file_id}/content`) e anexa o JSONL literal na mensagem de exceção para persistência no backend via `receive-result` com `errorMessage`. Também foi ampliado o parse da linha de saída (`BatchOutputLine`) para incluir o objeto `error` da OpenAI (`code`, `message`, `param`, `type`) em erros `status_code >= 400`, permitindo que a tela de detalhe da execução exiba causa raiz com maior precisão.
- 2026-05-09 15:25:00 (UTC-3): registro solicitado pelo usuário para consolidar o trabalho de persistência de detalhe técnico de falha no Gera Landing. Foi adicionado o campo `error_detail` (LONGTEXT) em `gera_landing_stage_execution` via Liquibase, com propagação ponta-a-ponta: payload de falha do `ai-worker` (`receiveFailure`) agora envia `errorDetail`, o backend passou a aceitar/persistir esse campo em `GeraLandingResultReceiveRequest`/`GeraLandingStageExecutionService`, e o detalhe também é retornado no endpoint de detalhe (`GeraLandingStageExecutionDetailResponse`). Objetivo: armazenar o JSON/causa raiz do erro OpenAI além da mensagem resumida.
- 2026-05-09 15:46:59 (UTC-3): registro complementar solicitado após revisão da UI de detalhe de execução. O frontend foi ajustado para exibir o campo `errorDetail` já persistido no backend, com inclusão do atributo no tipo `GeraLandingStageExecutionDetail` e renderização de bloco específico "Detalhe técnico do erro" na tela `ExperimentGeraLandingExecutionDetailPage`, garantindo visibilidade operacional do conteúdo técnico na análise do job sem consulta direta ao banco.
- 2026-05-09 16:05:00 (UTC-3): atendimento de revisão solicitado para desfazer alterações nos prompts `landing-copy.md`. Foram revertidas as mudanças recentes de regras/numeração e vocabulário de status em `ai-worker/src/main/resources/prompts/experiment/landing-copy.md` e `ai-worker/src/main/resources/prompts/geralanding/landing-page-copy.md`, restaurando o conteúdo da versão de referência anterior (commit `38e2064`). Também foi mantida a documentação de acompanhamento neste arquivo conforme política append-only.

- 2026-05-09 18:00:00 (UTC-3): registro solicitado para ajuste de usabilidade na tela de detalhe da execução Gera Landing (`frontend`). No bloco "Detalhe técnico do erro" de `ExperimentGeraLandingExecutionDetailPage`, foi adicionado botão "Copiar" ao lado do rótulo, reutilizando o fluxo existente de clipboard (`handleCopyJson`) e feedback visual (`Copiado!`) para facilitar compartilhamento e diagnóstico rápido do conteúdo técnico de falha.
- 2026-05-09 18:20:00 (UTC-3): registro solicitado após revisão do erro `invalid_json_schema` na etapa de copy da landing (Gera Landing). Foi consolidado o ajuste no schema `ai-worker/src/main/resources/prompts/geralanding/landing-page-copy-schema.json` para compatibilidade com validação estrita do `response_format` da OpenAI, incluindo a cobertura de chaves obrigatórias declaradas em objetos aninhados (com destaque para `hero.required` contendo `eyebrow`) e normalização de formatação no bloco `ctaBlocks.items.required`. Objetivo: evitar falha 400 de schema antes da geração e, consequentemente, reduzir ocorrências de batch finalizado sem `output_file_id`.
- 2026-05-10 18:10:00 (UTC-3): registro solicitado para confirmar e ajustar a exibição do detalhe técnico de erro em formato colapsável no frontend do Gera Landing. O componente `CollapsibleJsonViewer` recebeu a prop opcional `initiallyCollapsed` para controlar estado inicial do `<details>`, com propagação recursiva para nós filhos. Na página `ExperimentGeraLandingExecutionDetailPage`, o bloco "Detalhe técnico do erro" passou a renderizar o conteúdo extraído de `error_file` com `initiallyCollapsed` habilitado, garantindo que o JSON seja exibido em modo collapse por padrão e possa ser expandido pelo usuário durante a análise.
- 2026-05-10 09:30:00 (UTC-3): registro solicitado após nova revisão do ajuste de schema da etapa de copy da landing (Gera Landing). Foi complementado o contrato em `ai-worker/src/main/resources/prompts/geralanding/landing-page-copy-schema.json` para aderir ao strict mode do `response_format` da OpenAI no objeto raiz, incluindo no array `required` as chaves já definidas em `properties`: `messageMatchNotes`, `faq` e `complianceNotes`. Objetivo: eliminar rejeições `400 invalid_json_schema` por divergência entre `properties` e `required` e prevenir finalização de batch sem `output_file_id`.

- 2026-05-10 19:00:00 (UTC-3): correção de registro solicitada pelo usuário: a canonização do mecanismo de Assembler por etapa para HTML provisório (Wireframe, Copy, Image Briefing e Design Preset) foi registrada no arquivo correto `docs/gera-landing/registros1.md` (append-only). O conteúdo canônico permanece em `docs/gera-landing/modelo-canonico-gera-landing.md` na seção "12) Mecanismo canônico de Assembler por etapa para HTML provisório".

- 2026-05-10 14:50:00 (UTC-3): registro solicitado para validar compatibilidade entre implementação recente da etapa `landing-page-copy` e o documento canônico antes de consolidar o trabalho. Foi feita revisão de aderência em `docs/gera-landing/modelo-canonico-gera-landing.md` e identificado desvio na seção 1.2 (texto ainda declarava suporte oficial apenas para `landing-page-wireframe`). O documento canônico foi atualizado para refletir o estado atual do código com suporte a `landing-page-wireframe` + `landing-page-copy` no módulo Gera Landing. Também foi confirmado que o backend já calcula `provisional_html` por assembler na etapa de copy e persiste `landingPageCopy`/`landingPageCopyJobId` no experimento quando `receive-result` conclui sem erro.
- 2026-05-10 17:10:00 (UTC-3): ajuste solicitado pós-review para visibilidade consolidada de custo no frontend (`ExperimentDetailPage`, aba Gera Landing). Além dos totais por etapa (WireFrame e Copy), foi adicionado um total geral do Gera Landing somando as etapas concluídas (`landing-page-wireframe` + `landing-page-copy`) e exibido em card dedicado "Total Gera Landing (todas as etapas)" com formatação em USD. Objetivo: permitir leitura rápida do custo acumulado do fluxo completo sem depender da soma manual entre cards.
- 2026-05-10 17:35:00 (UTC-3): registro solicitado para consolidar a revisão do assembler da etapa `landing-page-copy` no backend (`CopyProvisionalHtmlAssembler`). A montagem do `provisional_html` deixou de usar limpeza heurística de lorem e passou a preencher **todos os nós textuais** do HTML (`h1`, `h2`, `h3`, `p`, `li`, `span`, `summary`, `a`, `button`) exclusivamente com os textos extraídos do JSON gerado na etapa de Gera Copy (`landingPageCopy`). Também foi aplicado fail-fast: quando não houver textos de copy ou quando a quantidade for insuficiente para preencher todos os campos textuais do wireframe, a montagem lança erro e retorna falha no fluxo de assemble, evitando preview com conteúdo parcial/inventado.
- 2026-05-10 17:20:00 (UTC-3): correção solicitada pelo usuário sobre o local do registro. O apontamento de trabalho referente ao ajuste de vazamento da etapa `landing-page-copy` foi consolidado no arquivo canônico `docs/gera-landing/registros1.md` (append-only), e o arquivo criado por engano fora da pasta de domínio foi removido para evitar duplicidade de histórico.
- 2026-05-11 10:45:00 (UTC-3): registro solicitado após erro OpenAI `400 invalid_json_schema` na etapa `landing-page-copy` com mensagem explícita de keyword não suportada (`not`) em `response_format`. Foi consultada a documentação oficial de Structured Outputs da OpenAI para confirmação do subconjunto suportado em `strict=true`, e o documento canônico `docs/gera-landing/modelo-canonico-gera-landing.md` foi atualizado com regra obrigatória de compatibilidade de schema (incluindo a lista de keywords de composição não suportadas: `allOf`, `not`, `dependentRequired`, `dependentSchemas`, `if`, `then`, `else`). Objetivo: prevenir novos envios de schema inválido no Gera Landing.
- 2026-05-11 10:52:00 (UTC-3): registro complementar solicitado para consolidar a governança de schema OpenAI no domínio Gera Landing. Foi formalizado no canônico que toda alteração futura em `text.format.schema` deve passar por consulta prévia da documentação oficial da OpenAI e ter evidência registrada neste arquivo (`registros1.md`) antes de publicação. Resultado esperado: redução de falhas de validação no Batch/Responses e melhoria de rastreabilidade operacional.
