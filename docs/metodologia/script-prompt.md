# Catalogo de prompts para criacao de pipelines

## Variaveis

| pipeline | versao | modulo-externo | pacote-backend | pacote-modulo | objeto-associado | objeto-chave | tabela-auditoria | endpoint-interno-base | endpoint-admin-base | objetivo-pipeline | arquivo-definicao |
|----------|--------|----------------|----------------|---------------|------------------|--------------|------------------|-----------------------|---------------------|--------------------|---------|
| nichocnae | v3 | oprm-coletor-mei | com.marketinghub.oprmcoletormei.nichocnae.v3 | com.marketinghub.pipelines.nichocnae.v3 | cnae | cnaeCode | pipeline_nichocnae | /api/internal/oprmcoletormei/nichocnae/v3 | /api/oprm/nichocnae/v3 | transformar um CNAE em persona, rotina diaria, tarefas, evidencias e materializacao de nicho enriquecido sem criar oferta ou landing |docs/marketing/pipeline_nichocnae_v3.md|
| dossieproduto | v1 | mois-sales-library-worker | com.marketinghub.moissaleslibraryworker.pipelines.dossieproduto.v1 | com.marketinghub.pipelines.dossie.v1 | pagina de venda | productKey | pipeline_dossieproduto | /api/internal/moissaleslibraryworker/dossieproduto/v1 | /api/mois/dossieproduto/v1 | transformar uma pagina de venda em dossie comercial auditavel para orientar decisoes de biblioteca de vendas |docs/marketing/pipeline_dossieproduto_v1.md|

## Premissas de uso

- Cada item iniciado com `*` e um prompt unico, independente e acionavel. Cada prompt deve solicitar uma tarefa completa, com entregavel proprio, sem servir apenas para preparar terreno para o prompt seguinte.
- Os prompts devem ser enviados ao modelo na sequencia deste documento.
- As variaveis no formato `{{variavel}}` devem ser substituidas pela tabela acima antes do envio.
- O modelo deve priorizar o padrao do `nichocnae`, porque ele tem melhor separacao entre backend fonte de verdade, modulo executor, catalogo de etapas, saidas funcionais e acompanhamento por tela.
- O `dossieproduto` deve ser usado como reforco para auditoria de request/response da OpenAI e para callbacks `recebeRequest`/`recebeResponse`.
- O backend principal nao executa etapa operacional nem chama OpenAI: ele inicia, persiste, publica pendencias, recebe resultados, valida e decide o avanco.
- O modulo externo executa as etapas, chama integracoes externas quando necessario, registra request/response no backend e nunca acessa banco diretamente.


## 1. Leitura obrigatoria do arquivo de definicao

* Antes de mapear, propor ou implementar qualquer etapa do pipeline `{{pipeline}}` `{{versao}}`, abra e leia integralmente o arquivo de definicao `{{arquivo-definicao}}`. Use esse arquivo como a fonte principal para entender o desenho esperado do pipeline: objetivo, fronteira de dominio, etapas oficiais, entradas, saidas, gates, artefatos, regras de avanco, responsabilidades do backend, responsabilidades do modulo executor e informacoes que precisam aparecer para o usuario. Nesta leitura, separe explicitamente envelope tecnico de dado de negocio: `stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma, erro e stack trace sao auditoria/envelope tecnico; diagnostico, evidencias comerciais, decisoes, artefatos funcionais, bloqueios de negocio, causa-raiz funcional e acao recomendada sao dado de negocio. Nao invente etapas a partir de padroes genericos enquanto houver definicao explicita nesse arquivo.

* Extraia do arquivo `{{arquivo-definicao}}` a lista oficial de etapas do pipeline `{{pipeline}}` `{{versao}}` antes de consultar o codigo. Para cada etapa definida no arquivo, registre: codigo da etapa, nome de negocio, objetivo, entrada obrigatoria, saida funcional esperada, artefatos, criterios de conclusao, criterios de bloqueio, proxima etapa permitida, endpoint esperado e modulo responsavel pela execucao. Para cada etapa, registre separadamente quais campos pertencem ao envelope tecnico/auditoria e quais pertencem ao dado funcional de negocio; nunca trate metadado tecnico como saida funcional esperada. Se o codigo existente divergir do arquivo de definicao, trate como lacuna a ser analisada e nao como autorizacao para ignorar a definicao.

* Use o arquivo `{{arquivo-definicao}}` para orientar a criacao ou ajuste das etapas. Cada controller, service, contrato, processor, prompt, schema, callback, registro de auditoria e card de tela deve nascer da definicao funcional da etapa, preservando a fronteira de dominio e a responsabilidade comercial descritas no arquivo. Ao criar qualquer contrato ou implementacao, mantenha campos tecnicos em envelope/auditoria separado e mantenha `resposta_final`, artefato final e saida funcional apenas com dado de negocio. Quando o arquivo exigir uma saida estruturada, gate, evidencia ou artefato, implemente isso como contrato persistido e auditavel, nao apenas como texto solto, log tecnico ou convencao implicita.

* Se o arquivo `{{arquivo-definicao}}` estiver ausente, incompleto ou contraditorio, pare antes de implementar e entregue uma lista objetiva das informacoes faltantes para fechar o contrato das etapas. Inclua como lacuna bloqueante qualquer ponto em que nao esteja claro o que e envelope tecnico/auditoria e o que e dado funcional de negocio. Nao avance criando pipeline, etapa, endpoint, prompt ou schema baseado apenas em inferencia quando faltar definicao de entrada, saida funcional, criterio de bloqueio ou proxima etapa permitida.

## 2. Descoberta e mapa do pipeline

* Antes de implementar qualquer codigo, faca uma varredura no repositorio para mapear o pipeline `{{pipeline}}` `{{versao}}`. Identifique o modulo executor real `{{modulo-externo}}`, o pacote backend `{{pacote-backend}}`, o pacote executor `{{pacote-modulo}}`, as etapas existentes, a ordem real de execucao, os endpoints internos, os endpoints administrativos, as tabelas envolvidas, a tela que inicia ou acompanha o fluxo e os documentos canonicos/registros relacionados. Entregue um resumo objetivo com: etapas em ordem, responsabilidade de cada etapa, entrada esperada, saida funcional, executor, endpoint `pending`, callback de request, callback de response, persistencia e lacunas encontradas. Nesse resumo, separe payload bruto, metadados tecnicos e dado funcional de negocio; a saida funcional nao pode conter `stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma ou stack trace como evidencia de negocio. Nao proponha implementacao ainda.

* Abra o arquivo `{{arquivo-definicao}}`, faca uma varredura propria no codigo do pipeline `{{pipeline}}` `{{versao}}` e compare as etapas encontradas com o objetivo do pipeline: `{{objetivo-pipeline}}`. Entregue uma avaliacao completa informando se cada etapa e suficiente para transformar a entrada do `{{objeto-associado}}` em uma saida de qualidade para a etapa seguinte e em um resultado final compreensivel para o usuario. Para cada etapa, responda: o que ela recebe, o que ela transforma, o que ela entrega, por que isso ajuda a venda ou a decisao comercial, quando deve bloquear e qual proxima etapa pode receber essa saida. Avalie tambem se a etapa mistura envelope tecnico com dado de negocio e recomende correcao quando metadados tecnicos aparecerem como resposta final, artefato funcional ou entrada funcional da etapa seguinte. Se alguma etapa estiver generica demais, faltando entrada, faltando saida estruturada, faltando gate ou gerando apenas status tecnico, recomende o ajuste antes de codificar.

* Abra o arquivo `{{arquivo-definicao}}`, valide o codigo existente quando necessario e defina o contrato funcional completo do pipeline `{{pipeline}}` `{{versao}}` como entregavel independente antes de qualquer implementacao. Monte uma tabela com as colunas: posicao, codigo da etapa, nome de negocio, objetivo comercial, executor, entrada obrigatoria, saida funcional estruturada, artefatos auditaveis, envelope tecnico/auditoria, criterio de conclusao, criterio de bloqueio, proxima etapa automatica, proxima acao humana quando houver e dados que precisam aparecer na tela. O contrato deve declarar que `resposta_final` e saida funcional estruturada recebem apenas dado de negocio, enquanto `stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma e erro ficam no envelope tecnico/auditoria. O contrato nao pode ter `nextStageCode` para etapa sem backend, endpoint `pending`, processor no executor e persistencia de auditoria.

## 3. Tabela de auditoria e modelo persistido

* Vamos criar ou ajustar a tabela de auditoria `{{tabela-auditoria}}` no backend para registrar cada passo do pipeline `{{pipeline}}` `{{versao}}`. A tabela deve permitir ao usuario entender pela tela o que aconteceu sem depender de logs. Ela deve conter no minimo: id, id_externo, job_id, job_id_externo, codigo_etapa, status, request, input_extraido_request, response, resposta_final, prompt, schema_json, plataforma, modelo, quantidade_token_entrada, quantidade_token_saida, custo, descricao_erro, versao_pipeline, data_hora, created_at e updated_at. Defina no proprio contrato da tabela que `request`, `response`, `prompt`, `schema_json`, modelo, tokens, custo, status e erro sao envelope tecnico/auditoria, enquanto `input_extraido_request` e `resposta_final` sao dados funcionais limpos; nao grave envelope tecnico dentro de `resposta_final`. Use Liquibase MySQL 5.7 em YAML, com preConditions `dbms:mysql`, `splitStatements: true`, `stripComments: true`, includes relativos com `relativeToChangelogFile: true`, e evite `UPDATE`/`DELETE` lendo a mesma tabela alvo em subconsulta para nao gerar erro 1093.

* Crie ou ajuste a entidade JPA e o repository da tabela `{{tabela-auditoria}}`. Declare `@Column(name = "...")` para todos os campos em que possa haver divergencia de nomenclatura. A entidade deve separar claramente payload bruto (`request`, `response`, `prompt`, `schema_json`) de entrada funcional extraida do request (`input_extraido_request`), saida funcional extraida do response (`resposta_final`) e de metadados tecnicos (`modelo`, tokens, custo, plataforma, erro). `resposta_final` nao pode armazenar envelope tecnico, ids de execucao, request, response, prompt, schema, tokens, custo, modelo, plataforma, erro tecnico ou stack trace. Adicione comentarios em portugues na classe e nos metodos alterados ou criados.

* Ajuste o objeto associado `{{objeto-associado}}` para possuir campos de acompanhamento do pipeline quando ainda nao existirem: status do pipeline, etapa atual, data da ultima atualizacao, job atual e referencia do resultado final quando fizer sentido. O status do objeto associado deve ser apenas um resumo operacional; a auditoria detalhada continua em `{{tabela-auditoria}}`. Nao copie request, response, prompt, schema, tokens, custo, stack trace ou envelope tecnico para campos de resultado funcional do objeto associado.

## 4. Backend: start, pending e callbacks

* No backend, dentro de `{{pacote-backend}}`, leia `{{arquivo-definicao}}` e implemente ou ajuste um controller e um service por etapa oficial do pipeline. Cada etapa deve expor endpoints internos no padrao `{{endpoint-interno-base}}/<codigo-etapa>/stage-executions`: `start` quando a etapa puder ser iniciada diretamente, `pending` para o modulo executor buscar trabalho, `recebeRequest` para registrar o request bruto antes da chamada externa ou processamento relevante, `recebeResponse` para registrar response bruto/resultado funcional/erro e endpoints de detalhe ou situacao usados pela tela. DTOs e services devem separar envelope tecnico/auditoria de dado funcional de negocio, impedindo que metadados tecnicos entrem em `resposta_final`, artefato final ou input funcional da proxima etapa. O endpoint publico/administrativo deve ficar em `{{endpoint-admin-base}}` ou no pacote administrativo ja existente do dominio.

* Leia `{{arquivo-definicao}}` para identificar a primeira etapa oficial e implemente o metodo `start` dessa etapa. Ele recebe a chave `{{objeto-chave}}` do `{{objeto-associado}}`, valida se o objeto existe, encerra ou substitui execucoes abertas quando a regra do dominio exigir novo ciclo, gera ou reutiliza um `jobId` rastreavel, persiste uma execucao `PENDING`/`INICIADO`, atualiza o resumo no objeto associado e nao chama o modulo executor diretamente. A resposta deve retornar `jobId`, etapa inicial, status, mensagem operacional e endpoint de acompanhamento como envelope operacional, sem tratar esses campos como resultado funcional de negocio.

* Leia `{{arquivo-definicao}}` para identificar as etapas oficiais e implemente o metodo `pending` de cada etapa. Ele deve retornar ate 10 execucoes pendentes ordenadas da mais antiga para a mais nova, com contexto completo para o executor trabalhar sem consultar banco: `stageExecutionId`, `jobId`, `{{objeto-chave}}`, dados do `{{objeto-associado}}`, inputPayload da etapa, artefatos anteriores necessarios, attemptNumber quando existir, status atual e versao do pipeline. O `pending` deve separar campos de controle/envelope tecnico dos campos funcionais; artefatos tecnicos anteriores nao podem ser enviados como entrada de negocio da etapa seguinte. O pending deve ser o unico ponto inicial de consumo do modulo `{{modulo-externo}}`.

* Leia `{{arquivo-definicao}}` para identificar as etapas oficiais e implemente o callback `recebeRequest` de cada etapa. Ele deve gravar em `{{tabela-auditoria}}` o request bruto enviado ou recebido pelo executor, com `id_externo={{objeto-chave}}`, `job_id`, `codigo_etapa`, `status=AGUARDANDO_RETORNO_MODULO`, `request`, `input_extraido_request`, `prompt`, `schema_json`, `plataforma`, `modelo`, `versao_pipeline={{versao}}` e data-hora atual. O `request`, prompt, schema, plataforma, modelo, status e ids pertencem ao envelope tecnico/auditoria; `input_extraido_request` deve conter apenas entrada funcional limpa que pode ser explicada ao usuario ou usada como negocio. Tambem deve atualizar o resumo do `{{objeto-associado}}` para mostrar que a etapa esta em execucao. Esse callback deve aceitar request completo, nao apenas resumo.

* Leia `{{arquivo-definicao}}` para identificar as etapas oficiais e implemente o callback `recebeResponse` de cada etapa. Ele deve gravar em `{{tabela-auditoria}}` o response bruto completo, extrair e persistir `resposta_final` limpa quando houver resposta funcional, registrar tokens, custo, modelo, plataforma e descricao de erro, atualizar o status da etapa para concluida, falha ou bloqueada, e atualizar o resumo do `{{objeto-associado}}`. `resposta_final` nao pode conter `stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma, erro tecnico ou stack trace; esses dados devem ficar apenas no envelope tecnico/auditoria. O custo deve ser calculado pelo backend como fonte de verdade, usando `modelo`, `quantidade_token_entrada`, `quantidade_token_saida`, `service_tier`/modo de execucao quando existir e os precos do catalogo `openai_model` em USD por 1 milhao de tokens; o custo enviado pelo executor deve ser aceito apenas como legado/auditoria e nunca como valor autoritativo quando houver preco cadastrado. Se o resultado funcional estiver valido e a proxima etapa for automatica, o backend deve enfileirar a proxima etapa. Se houver erro, contrato invalido, bloqueio comercial, ausencia de evidencia ou etapa final, nao avance automaticamente.

* Garanta que o backend seja a fonte de verdade da transicao. O modulo executor pode enviar `nextStageCode` apenas como sugestao/auditoria, mas o service do backend deve validar se essa proxima etapa existe no contrato, se a etapa atual concluiu com sucesso funcional, se nao ha gate pendente e se o resultado funcional limpo da etapa pode alimentar a etapa seguinte. Nao use envelope tecnico, payload bruto ou metadado de auditoria como evidencia de negocio para avancar. Se a validacao falhar, persistir bloqueio com causa-raiz e acao recomendada.

## 5. Modulo executor

* No modulo `{{modulo-externo}}`, crie ou ajuste o nucleo generico do pipeline no pacote `{{pacote-modulo}}` ou no pacote ja canonico do executor. O nucleo deve conter apenas contratos e orquestracao generica, como `PipelineWorker`, `StageProcessor`, `StageContext`, `StageResult`, `StageArtifact`, cliente de backend e catalogo de etapas. `StageResult` e `StageArtifact` devem separar saida funcional, artefatos auditaveis e envelope tecnico, sem colocar request, response, prompt, schema, tokens, custo, modelo, plataforma, erro ou ids de execucao dentro da saida funcional de negocio. O nucleo nao pode depender de etapa concreta nem de tecnologia externa especifica como OpenAI, WebClient operacional, Jsoup, Playwright, Selenium ou S3.

* No modulo `{{modulo-externo}}`, leia `{{arquivo-definicao}}` e crie ou ajuste um processor por etapa concreta do pipeline `{{pipeline}}` `{{versao}}`. Cada processor deve implementar o contrato de etapa, consumir apenas o `StageContext` com dados vindos do backend, validar entradas obrigatorias, produzir saida funcional estruturada, gerar artefatos auditaveis e nunca importar outra etapa concreta. O processor deve tratar metadados tecnicos como envelope/auditoria e nunca como evidencia comercial, resposta final ou entrada funcional da proxima etapa. Quando precisar de resultado anterior, use o input persistido no backend ou artefato auditavel entregue pelo `pending`.

* Leia `{{arquivo-definicao}}` e crie ou ajuste o catalogo de etapas do executor em ordem operacional. O catalogo deve registrar somente etapas com contrato completo no backend e processor implementado. Para cada etapa, vincule `codigo_etapa`, endpoint `pending` em `{{endpoint-interno-base}}/<codigo-etapa>/stage-executions`, processor e regra de output. A regra de output deve declarar separadamente saida funcional, artefatos e envelope tecnico/auditoria, bloqueando etapa que tente entregar metadados tecnicos como resultado de negocio. O executor deve percorrer o catalogo, chamar `pending`, executar os jobs e reportar resultado ao backend.

* Para toda etapa com IA ou integracao externa, mova prompt operacional e schema JSON para arquivos versionados em `src/main/resources/prompts/{{pipeline}}{{versao}}/`. A classe deve apenas carregar prompt/schema do classpath, preencher placeholders com contexto persistido pelo backend, montar request, chamar a integracao, validar response e devolver auditoria. O schema de resposta deve separar envelope tecnico de saida funcional e proibir `stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma ou erro tecnico dentro do artefato final de negocio. Nao deixe prompt longo, schema ou contrato de saida hardcoded em Java ou TypeScript. Para OpenAI, use `service_tier: "flex"` por padrao, salvo excecao funcional registrada.

* Antes de qualquer chamada OpenAI ou integracao externa, o executor deve enviar ao backend o `recebeRequest` com request bruto completo, prompt final, schema, plataforma, modelo e `jobId`. Depois da resposta ou erro, deve enviar `recebeResponse` com response bruto completo, saida funcional validada, tokens de entrada, tokens de saida, modelo, `service_tier`/modo de execucao, erro e status. O executor deve enviar payload bruto e metadados tecnicos apenas nos campos de auditoria/envelope tecnico, e deve enviar resposta funcional limpa em campo separado. O executor pode enviar custo estimado somente como dado auxiliar, mas o backend deve recalcular e persistir o custo autoritativo usando o catalogo `openai_model`. A tela deve conseguir mostrar esses dados a partir do backend, com payload bruto em detalhe colapsado e resposta funcional visivel em linguagem de negocio.

## 6. Qualidade das saidas e encadeamento

* Leia `{{arquivo-definicao}}` e revise a saida funcional de cada etapa do pipeline `{{pipeline}}` `{{versao}}`. Ela deve ser boa o suficiente para ser entrada da etapa seguinte, nao apenas um texto livre ou status tecnico. Para cada etapa, separe o envelope tecnico (`stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma e erro tecnico) da saida funcional de negocio. A saida funcional deve conter apenas campos como resumo de negocio, evidencias usadas, artefatos funcionais gerados, criterios atendidos, bloqueios de negocio, causa-raiz funcional quando houver, acao recomendada, `businessBoundary` quando houver limite de dominio, `reportRole` e sugestao funcional de proxima etapa. Se uma resposta final for consumivel pelo usuario, grave tambem em `resposta_final` sem envelope tecnico.

* Inclua gates funcionais compativeis com o risco do pipeline. O gate deve bloquear avanco quando faltar evidencia, quando a resposta do modelo estiver fora do schema, quando a saida contaminar o artefato final com metadados tecnicos, quando houver baixa qualidade comercial ou quando a etapa tentar invadir uma responsabilidade de pipeline posterior. O gate deve validar que `resposta_final`, saida funcional e artefato final nao contenham `stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma, erro tecnico ou stack trace. O bloqueio deve gravar causa-raiz, impacto comercial e etapa recomendada de correcao.

* Garanta que o pipeline preserve o limite de dominio. Para `nichocnae`, nao gerar oferta, campanha, promessa, landing page ou hipotese; o resultado e persona, rotina, tarefas, evidencias e materializacao de nicho. Para `dossieproduto`, nao transformar o dossie em publicacao automatica sem gate; o resultado e diagnostico comercial auditavel da pagina de venda. Esse limite deve ser aplicado sobre dados funcionais de negocio; envelope tecnico, logs, request, response, prompt, schema, tokens, custo e ids de execucao nao podem ser usados como substitutos de evidencia comercial.

## 7. Tela de inicio e acompanhamento

* Ajuste a tela que inicia o pipeline `{{pipeline}}` `{{versao}}` para chamar o endpoint `start` da primeira etapa passando `{{objeto-chave}}` do `{{objeto-associado}}`. A tela deve mostrar claramente que o pipeline foi iniciado, o `jobId`, a etapa inicial, o status geral e um caminho direto para acompanhar o andamento como informacao operacional, sem misturar esses campos com resultado funcional de negocio. Nao chame endpoints internos de worker pelo frontend.

* Crie ou ajuste a tela de acompanhamento do pipeline `{{pipeline}}` `{{versao}}`. Ela deve consumir endpoint administrativo do backend, nao logs. A tela deve ter: cabecalho com contexto do `{{objeto-associado}}`, objetivo do pipeline, status geral, proxima acao principal, lista de cards em ordem real das etapas, area final com resultado consolidado e historico de execucoes. Cada card deve mostrar nome de negocio, objetivo, status, entrada resumida, saida funcional, ultima execucao, erro/bloqueio em linguagem simples, custo quando houver, e botao para ver detalhes. A tela deve separar visualmente dado funcional de negocio de envelope tecnico/auditoria; metadados como jobId, stageExecutionId, request, response, prompt, schema, tokens, custo e stack trace nao podem aparecer como conteudo principal do resultado.

* No detalhe de cada etapa da tela, mostre primeiro a decisao e o artefato funcional. Em seguida, mostre evidencias, entrada usada, input extraido do request, texto extraido do response, resposta final limpa, custo e status. Request bruto, response bruto, prompt, schema, tokens, payload completo e stack trace devem ficar em area tecnica colapsada. O usuario precisa entender o que esta acontecendo sem ler JSON bruto.

* Garanta que a tela traduza estados tecnicos para linguagem operacional: `PENDING` como "Na fila", `RUNNING` ou `AGUARDANDO_RETORNO_MODULO` como "Em execucao", `COMPLETED` ou `CONCLUIDO` como "Concluida", `FAILED` ou `FALHA` como "Falhou", `BLOCKED` como "Bloqueada" e `SUPERSEDED` como "Substituida". Esses estados sao envelope operacional e nao devem ser exibidos como evidencia comercial, diagnostico, artefato final ou resposta funcional. Nao mostre progresso falso quando o backend nao persistiu a etapa.

## 8. Documentacao, testes e prevencao de recorrencia

* Atualize a documentacao canonica ou o registro do tema do pipeline `{{pipeline}}` `{{versao}}` com: objetivo, pacote backend, pacote executor, modulo externo, etapas oficiais, endpoints internos, endpoint administrativo de acompanhamento, tabela de auditoria, regras de avanco, gates e como a tela apresenta request/response ao usuario. Documente tambem a separacao obrigatoria entre envelope tecnico/auditoria e dado funcional de negocio, incluindo que `resposta_final`, saida funcional e artefato final nao podem conter request, response, prompt, schema, tokens, custo, modelo, plataforma, erro tecnico, stack trace ou ids de execucao. Se a mudanca alterar regra operacional, atualize o documento em `/docs/canonical`.

* Adicione ou ajuste testes do backend para validar `start`, `pending`, `recebeRequest`, `recebeResponse`, persistencia em `{{tabela-auditoria}}`, preenchimento de `resposta_final`, calculo de `custo` pelo backend a partir de modelo, tokens e precos de `openai_model`, atualizacao do status do `{{objeto-associado}}`, enfileiramento da proxima etapa e bloqueio quando houver erro ou contrato invalido. Os testes devem provar que `resposta_final`, saida funcional e artefato final nao recebem `stage`, `status`, `jobId`, `stageExecutionId`, request, response, prompt, schema, tokens, custo, modelo, plataforma, erro tecnico ou stack trace. Em Java, todos os comentarios de classes e metodos alterados devem estar em portugues.

* Adicione ou ajuste testes do modulo `{{modulo-externo}}` para validar que o executor consome somente `pending`, registra `recebeRequest` antes de OpenAI/integracao externa, registra `recebeResponse` depois, valida schema, gera saida funcional estruturada, nao acessa banco, nao importa etapas concretas entre si e nao coloca tecnologia externa no nucleo generico. Os testes devem garantir que `StageResult`, `StageArtifact`, callbacks e schema mantem envelope tecnico separado de dado funcional de negocio e bloqueiam metadados tecnicos dentro da saida funcional.

* Adicione ou ajuste testes de frontend para validar que a tela inicia o pipeline pelo endpoint administrativo correto, mostra cards em ordem, exibe saida funcional e resposta final, exibe input extraido do request e texto extraido do response, mantem request/response bruto em detalhe tecnico colapsado, apresenta erro/bloqueio com causa e nao depende de logs para explicar o andamento. Os testes devem garantir que request, response, prompt, schema, tokens, custo, modelo, plataforma, stack trace, `jobId` e `stageExecutionId` nao aparecem como conteudo principal do resultado funcional.

* Antes de concluir, revise a causa-raiz que motivou qualquer ajuste encontrado: contrato incompleto, etapa sem saida funcional, auditoria sem request/response, mistura de envelope tecnico com dado de negocio, backend sem decisao de avanco, tela baseada em log ou pacote divergente. Corrija a causa-raiz quando estiver no escopo; se exigir decisao do usuario, registre objetivamente qual decisao falta e qual risco permanece. A conclusao deve informar explicitamente se ainda existe risco de request, response, prompt, schema, tokens, custo, erro tecnico ou ids de execucao contaminarem `resposta_final`, artefato final ou saida funcional.
