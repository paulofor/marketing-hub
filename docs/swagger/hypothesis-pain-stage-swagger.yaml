openapi: 3.0.3
info:
  title: Hypothesis Pipeline Stages API
  version: 1.0.0
  description: Contratos das etapas Dor, Resultado, Mecanismo, Prova e Oferta do pipeline de hipótese.
paths:
  /api/niches/{nicheId}/hypothesis-pipeline/summary:
    get:
      summary: Lista o conteúdo final concluído de cada etapa do framework de hipótese.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: Conteúdos finais retornados com origem no banco de dados.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/HypothesisStageFinalSummaryResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/pain/start:
    post:
      summary: Inicia a construção auditável da dor do nicho.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '202':
          description: Execução iniciada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainStartResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/pain/stage-executions:
    get:
      summary: Lista execuções da etapa Dor para acompanhamento na tela de nova hipótese.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: query
          name: includeCompleted
          schema: { type: boolean, default: true }
      responses:
        '200':
          description: Execuções retornadas.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/HypothesisPainExecutionSummaryResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/pain/stage-executions/{idJob}:
    get:
      summary: Consulta detalhe auditável de uma execução da etapa Dor para a tela de detalhe do job.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Detalhe da execução retornado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainExecutionDetailResponse'
  /api/internal/hypothesis-pipeline/pain/stage-executions/pending:
    get:
      summary: Lista jobs pendentes da etapa Dor para o Worker AI.
      responses:
        '200':
          description: Jobs pendentes.
  /api/internal/hypothesis-pipeline/pain/stage-executions/{idJob}/running:
    post:
      summary: Marca uma execução como em processamento.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '202': { description: Execução marcada. }
  /api/internal/hypothesis-pipeline/pain/stage-executions/{idJob}/recebe-prompt:
    post:
      summary: Recebe prompt, schema e request cru enviados para a OpenAI.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebePromptRequest'
      responses:
        '202': { description: Prompt registrado. }
  /api/internal/hypothesis-pipeline/pain/stage-executions/{idJob}/recebe-resposta:
    post:
      summary: Recebe resposta ou erro da OpenAI para concluir a etapa Dor.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebeRespostaRequest'
      responses:
        '202': { description: Resposta registrada. }
  /api/niches/{nicheId}/hypothesis-pipeline/result/start:
    post:
      summary: Inicia a construção auditável do resultado desejado do nicho.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '202':
          description: Execução iniciada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainStartResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/result/stage-executions:
    get:
      summary: Lista execuções da etapa Resultado para acompanhamento na tela de nova hipótese.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: query
          name: includeCompleted
          schema: { type: boolean, default: true }
      responses:
        '200':
          description: Execuções retornadas.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/HypothesisPainExecutionSummaryResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/result/stage-executions/{idJob}:
    get:
      summary: Consulta detalhe auditável de uma execução da etapa Resultado para a tela de detalhe do job.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Detalhe da execução retornado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainExecutionDetailResponse'
  /api/internal/hypothesis-pipeline/result/stage-executions/pending:
    get:
      summary: Lista jobs pendentes da etapa Resultado para o Worker AI.
      responses:
        '200':
          description: Jobs pendentes.
  /api/internal/hypothesis-pipeline/result/stage-executions/{idJob}/running:
    post:
      summary: Marca uma execução de resultado como em processamento.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '202': { description: Execução marcada. }
  /api/internal/hypothesis-pipeline/result/stage-executions/{idJob}/recebe-prompt:
    post:
      summary: Recebe prompt, schema e request cru da etapa Resultado enviados para a OpenAI.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebePromptRequest'
      responses:
        '202': { description: Prompt registrado. }
  /api/internal/hypothesis-pipeline/result/stage-executions/{idJob}/recebe-resposta:
    post:
      summary: Recebe resposta ou erro da OpenAI para concluir a etapa Resultado.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebeRespostaRequest'
      responses:
        '202': { description: Resposta registrada. }
  /api/niches/{nicheId}/hypothesis-pipeline/mechanism/start:
    post:
      summary: Inicia a construção auditável do mecanismo do nicho.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '202':
          description: Execução iniciada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainStartResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/mechanism/stage-executions:
    get:
      summary: Lista execuções da etapa Mecanismo para acompanhamento na tela de nova hipótese.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: query
          name: includeCompleted
          schema: { type: boolean, default: true }
      responses:
        '200':
          description: Execuções retornadas.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/HypothesisPainExecutionSummaryResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/mechanism/stage-executions/{idJob}:
    get:
      summary: Consulta detalhe auditável de uma execução da etapa Mecanismo para a tela de detalhe do job.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Detalhe da execução retornado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainExecutionDetailResponse'
  /api/internal/hypothesis-pipeline/mechanism/stage-executions/pending:
    get:
      summary: Lista jobs pendentes da etapa Mecanismo para o Worker AI.
      responses:
        '200':
          description: Jobs pendentes.
  /api/internal/hypothesis-pipeline/mechanism/stage-executions/{idJob}/running:
    post:
      summary: Marca uma execução de mecanismo como em processamento.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '202': { description: Execução marcada. }
  /api/internal/hypothesis-pipeline/mechanism/stage-executions/{idJob}/recebe-prompt:
    post:
      summary: Recebe prompt, schema e request cru da etapa Mecanismo enviados para a OpenAI.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebePromptRequest'
      responses:
        '202': { description: Prompt registrado. }
  /api/internal/hypothesis-pipeline/mechanism/stage-executions/{idJob}/recebe-resposta:
    post:
      summary: Recebe resposta ou erro da OpenAI para concluir a etapa Mecanismo.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebeRespostaRequest'
      responses:
        '202': { description: Resposta registrada. }
  /api/niches/{nicheId}/hypothesis-pipeline/proof/start:
    post:
      summary: Inicia a construção auditável da prova do nicho.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '202':
          description: Execução iniciada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainStartResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/proof/stage-executions:
    get:
      summary: Lista execuções da etapa Prova para acompanhamento na tela de nova hipótese.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: query
          name: includeCompleted
          schema: { type: boolean, default: true }
      responses:
        '200':
          description: Execuções retornadas.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/HypothesisPainExecutionSummaryResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/proof/stage-executions/{idJob}:
    get:
      summary: Consulta detalhe auditável de uma execução da etapa Prova para a tela de detalhe do job.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Detalhe da execução retornado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainExecutionDetailResponse'
  /api/internal/hypothesis-pipeline/proof/stage-executions/pending:
    get:
      summary: Lista jobs pendentes da etapa Prova para o Worker AI.
      responses:
        '200':
          description: Jobs pendentes.
  /api/internal/hypothesis-pipeline/proof/stage-executions/{idJob}/running:
    post:
      summary: Marca uma execução de prova como em processamento.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '202': { description: Execução marcada. }
  /api/internal/hypothesis-pipeline/proof/stage-executions/{idJob}/recebe-prompt:
    post:
      summary: Recebe prompt, schema e request cru da etapa Prova enviados para a OpenAI.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebePromptRequest'
      responses:
        '202': { description: Prompt registrado. }
  /api/internal/hypothesis-pipeline/proof/stage-executions/{idJob}/recebe-resposta:
    post:
      summary: Recebe resposta ou erro da OpenAI para concluir a etapa Prova.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebeRespostaRequest'
      responses:
        '202': { description: Resposta registrada. }
  /api/niches/{nicheId}/hypothesis-pipeline/offer/start:
    post:
      summary: Inicia a construção auditável da oferta do nicho.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '202':
          description: Execução iniciada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainStartResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/offer/stage-executions:
    get:
      summary: Lista execuções da etapa Oferta para acompanhamento na tela de nova hipótese.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: query
          name: includeCompleted
          schema: { type: boolean, default: true }
      responses:
        '200':
          description: Execuções retornadas.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/HypothesisPainExecutionSummaryResponse'
  /api/niches/{nicheId}/hypothesis-pipeline/offer/stage-executions/{idJob}:
    get:
      summary: Consulta detalhe auditável de uma execução da etapa Oferta para a tela de detalhe do job.
      parameters:
        - in: path
          name: nicheId
          required: true
          schema: { type: integer, format: int64 }
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Detalhe da execução retornado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HypothesisPainExecutionDetailResponse'
  /api/internal/hypothesis-pipeline/offer/stage-executions/pending:
    get:
      summary: Lista jobs pendentes da etapa Oferta para o Worker AI.
      responses:
        '200':
          description: Jobs pendentes.
  /api/internal/hypothesis-pipeline/offer/stage-executions/{idJob}/running:
    post:
      summary: Marca uma execução de oferta como em processamento.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      responses:
        '202': { description: Execução marcada. }
  /api/internal/hypothesis-pipeline/offer/stage-executions/{idJob}/recebe-prompt:
    post:
      summary: Recebe prompt, schema e request cru da etapa Oferta enviados para a OpenAI.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebePromptRequest'
      responses:
        '202': { description: Prompt registrado. }
  /api/internal/hypothesis-pipeline/offer/stage-executions/{idJob}/recebe-resposta:
    post:
      summary: Recebe resposta ou erro da OpenAI para concluir a etapa Oferta.
      parameters:
        - in: path
          name: idJob
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecebeRespostaRequest'
      responses:
        '202': { description: Resposta registrada. }
components:
  schemas:
    HypothesisPainStartResponse:
      type: object
      required: [jobid, status]
      properties:
        jobid: { type: string }
        status: { type: string }
    HypothesisStageFinalSummaryResponse:
      type: object
      properties:
        slug: { type: string }
        stageNumber: { type: integer }
        stageTitle: { type: string }
        stageCode: { type: string }
        jobid: { type: string, nullable: true }
        status: { type: string, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        finalContent: { type: string, nullable: true }
        sourceTable: { type: string }
        sourceField: { type: string }
    HypothesisPainExecutionSummaryResponse:
      type: object
      properties:
        jobid: { type: string }
        marketNicheId: { type: integer, format: int64 }
        hypothesisId: { type: string, nullable: true }
        stageCode: { type: string }
        status: { type: string }
        executionRequestedAt: { type: string, format: date-time }
        processingStartedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        openAiModel: { type: string, nullable: true }
        openAiJobId: { type: string, nullable: true }
        inputTokens: { type: integer, nullable: true }
        outputTokens: { type: integer, nullable: true }
        costUsd: { type: number, nullable: true }
        errorMessage: { type: string, nullable: true }
        modelResponse: { type: string, nullable: true }
    HypothesisPainExecutionDetailResponse:
      type: object
      properties:
        jobid: { type: string }
        marketNicheId: { type: integer, format: int64 }
        hypothesisId: { type: string, nullable: true }
        stageCode: { type: string }
        status: { type: string }
        executionRequestedAt: { type: string, format: date-time }
        processingStartedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        promptTemplateId: { type: string, nullable: true }
        promptContent: { type: string, nullable: true }
        prompt: { type: string, nullable: true }
        promptMarkdownContent: { type: string, nullable: true }
        schemaJson: { type: string, nullable: true }
        openAiRequestBody: { type: string, nullable: true }
        openAiModel: { type: string, nullable: true }
        openAiJobId: { type: string, nullable: true }
        rawResponse: { type: string, nullable: true }
        modelResponse: { type: string, nullable: true }
        errorMessage: { type: string, nullable: true }
        errorDetail: { type: string, nullable: true }
        inputTokens: { type: integer, nullable: true }
        outputTokens: { type: integer, nullable: true }
        costUsd: { type: number, nullable: true }
    RecebePromptRequest:
      type: object
      required: [prompt, schemaJson, requestBodyJson]
      properties:
        prompt: { type: string }
        promptMarkdownContent: { type: string, nullable: true }
        schemaJson: { type: string }
        requestBodyJson: { type: string }
        openAiModel: { type: string, nullable: true }
        jobidopenai: { type: string, nullable: true }
    RecebeRespostaRequest:
      type: object
      properties:
        marketNicheId: { type: integer, format: int64 }
        stageCode: { type: string }
        modelResponse: { type: string, nullable: true }
        rawResponse: { type: string, nullable: true }
        inputTokens: { type: integer, nullable: true }
        outputTokens: { type: integer, nullable: true }
        costUsd: { type: number, nullable: true }
        openAiJobId: { type: string, nullable: true }
        errorMessage: { type: string, nullable: true }
        errorDetail: { type: string, nullable: true }
