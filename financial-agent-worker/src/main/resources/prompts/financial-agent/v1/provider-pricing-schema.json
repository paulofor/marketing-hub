{
  "type": "object",
  "additionalProperties": false,
  "required": ["amountUsd", "unit", "quantity", "resolution", "includesAudio", "sourceUrl", "observedAt", "status", "notes"],
  "properties": {
    "amountUsd": {"type": ["number", "null"], "exclusiveMinimum": 0},
    "unit": {"type": "string", "enum": ["SECOND", "VIDEO", "CREDIT"]},
    "quantity": {"type": ["number", "null"], "exclusiveMinimum": 0},
    "resolution": {"type": ["string", "null"]},
    "includesAudio": {"type": ["boolean", "null"]},
    "sourceUrl": {"type": "string", "minLength": 8},
    "observedAt": {"type": "string", "format": "date-time"},
    "status": {"type": "string", "enum": ["VERIFIED", "INCOMPARABLE", "BLOCKED"]},
    "notes": {"type": ["string", "null"]}
  }
}
