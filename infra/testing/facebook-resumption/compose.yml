services:
  mysql:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: local-resumption-only
      MYSQL_DATABASE: resumption_test
    ports:
      - "13391:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-plocal-resumption-only"]
      interval: 3s
      timeout: 3s
      retries: 30
