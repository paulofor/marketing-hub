server:
  port: 8096

spring:
  application:
    name: pde-platform-backend

pde:
  access:
    storage-path: ${PDE_ACCESS_STORAGE_PATH:/data/pde/access-grants.json}

management:
  endpoints:
    web:
      exposure:
        include: health,info
