# Controle negativo: reproduz a resolução estática das imagens anteriores.
server {
  listen 80;
  proxy_connect_timeout 2s;
  location /api/ {
    proxy_pass http://${PDE_BACKEND_UPSTREAM}/api/;
  }
}
